# Documentation

- `backend-implementation-plan.md` contains the backend planning baseline.
- `adr/` is reserved for architecture decisions.
- `api/` is reserved for endpoint and payload contracts.
- `operations/` is reserved for admin and support runbooks.
