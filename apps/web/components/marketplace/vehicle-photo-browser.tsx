import { PhotoGallery, type PhotoGalleryProps } from "@/components/marketplace/photo-gallery";

export function VehiclePhotoBrowser(props: PhotoGalleryProps) {
  return <PhotoGallery {...props} />;
}
